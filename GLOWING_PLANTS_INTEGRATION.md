# ✅ Светящиеся растения добавлены в Subnautica пример

## Что изменилось

### examples/subnautica/main.go
- ❌ Удалены старые компоненты `transform` и `plantVisual`
- ✅ Используются новые `objects.Transform` и `objects.GlowPlant`
- ✅ Растения теперь создаются через `objects.SpawnRandomGlowingPlants()`
- ✅ Рендеринг через `ctx.GlowRenderer` с эмиссивным шейдером
- ✅ 180 светящихся растений со случайными цветами и пульсацией

## Запуск

```bash
go run ./examples/subnautica
```

## Результат

Теперь в подводной сцене вместо обычных растений светятся неоновые растения:
- 🟢 Зелёные, 🔵 синие, 🟣 фиолетовые, 🩷 розовые, 🟠 оранжевые, 🩵 голубые
- Пульсация с разной скоростью
- Rim lighting эффект для объёмности
- Видны в темноте и под водой

Ничего не сломано - вся остальная функциональность работает как прежде! ✨
